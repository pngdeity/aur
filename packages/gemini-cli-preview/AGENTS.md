# Specialized Mandates: gemini-cli-preview

## 1. Research & Documentation
- **Upstream Source**: When retrieving information about this package's build system, testing, or any topic relevant to the ABS files here, you MUST reference the official source repository: https://github.com/google-gemini/gemini-cli

## 2. Maintenance
- **State**: This package tracks the `preview` release tier; ensure `.nvchecker.toml` is configured to ignore stable release tags if they conflict.
- **Consistency**: Maintain parity with the `gemini-cli` stable package functions for `build()` and `package()`.
