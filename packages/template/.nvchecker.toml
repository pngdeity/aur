# UPSTREAM VERSION TRACKING
# This configuration defines the discovery target for automated updates.

[template-package]
source = "github"
github = "example/software-name"
use_latest_tag = true
prefix = "v"

# Derived via the Bootstrap Workflow (see root AGENTS.md for documentation requirements).
