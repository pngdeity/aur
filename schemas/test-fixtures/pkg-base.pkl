import "../arch_pkg.pkl" as pkg

output {
  value = new pkg.Package {
    pkgname = "testpkg"
    pkgver = "1.0"
    pkgrel = 1
    pkgdesc = "base description"
    arch = new { "x86_64" }
    url = "https://example.com"
    license = new { "MIT" }
    depends = new { "glibc" }
    prepare = "echo base_prepare"
    build = "echo base_build"
    maintainer = "pngdeity <pngdeity@tutanota.com>"
  }
}
